# .gitignore is honoured automatically, so build output (.nuxt, .output, dist,
# .data) and node_modules do not need listing here.

# The Shiki grammar is data, not source. Reformatting is harmless but buries
# every real change to Rux highlighting in a whole-file diff.
grammars/rux.tmLanguage.json

# npm owns this file's formatting.
package-lock.json
