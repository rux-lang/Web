# `system`

Calls the host environment's command processor.

**Package:** `C`

**C reference:** [`system`](https://en.cppreference.com/c/program/system)

## Signature

```rux
func system(command: *char8) -> int32;
```

## See also

- [`C`](/api/c/) — the package overview
