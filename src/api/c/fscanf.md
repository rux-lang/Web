# `fscanf`

Reads formatted input from a file stream.

**Package:** `C`

**C reference:** [`fscanf`](https://en.cppreference.com/c/io/fscanf)

## Signature

```rux
func fscanf(stream: *opaque, format: *char8, ...) -> int32;
```

## See also

- [`C`](/api/c/) — the package overview
