# `realloc`

Changes the size of the memory block pointed to by ptr to size bytes.

**Package:** `C`

**C reference:** [`realloc`](https://en.cppreference.com/c/memory/realloc)

## Signature

```rux
func realloc(ptr: *opaque, size: uint) -> *opaque;
```

## See also

- [`C`](/api/c/) — the package overview
