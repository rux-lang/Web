# `ungetc`

Puts a character back into the given stream.

**Package:** `C`

**C reference:** [`ungetc`](https://en.cppreference.com/c/io/ungetc)

## Signature

```rux
func ungetc(ch: int32, stream: *opaque) -> int32;
```

## See also

- [`C`](/api/c/) — the package overview
