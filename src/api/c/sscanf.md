# `sscanf`

Reads formatted input from a string.

**Package:** `C`

**C reference:** [`sscanf`](https://en.cppreference.com/c/io/sscanf)

## Signature

```rux
func sscanf(buffer: *char8, format: *char8, ...) -> int32;
```

## See also

- [`C`](/api/c/) — the package overview
