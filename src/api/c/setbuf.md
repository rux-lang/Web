# `setbuf`

Sets the buffer to be used by the given stream.

**Package:** `C`

**C reference:** [`setbuf`](https://en.cppreference.com/c/io/setbuf)

## Signature

```rux
func setbuf(stream: *opaque, buffer: *char8);
```

## See also

- [`C`](/api/c/) — the package overview
