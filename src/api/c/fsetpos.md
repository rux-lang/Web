# `fsetpos`

Sets the file position of the stream to the given position.

**Package:** `C`

**C reference:** [`fsetpos`](https://en.cppreference.com/c/io/fsetpos)

## Signature

```rux
func fsetpos(stream: *opaque, pos: *opaque) -> int32;
```

## See also

- [`C`](/api/c/) — the package overview
