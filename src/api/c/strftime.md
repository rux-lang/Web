# `strftime`

Converts a tm object to a custom textual representation.

**Package:** `C`

**C reference:** [`strftime`](https://en.cppreference.com/c/chrono/strftime)

## Signature

```rux
func strftime(str: *char8, count: uint, format: *char8, time_ptr: *tm) -> uint;
```

## See also

- [`C`](/api/c/) — the package overview
