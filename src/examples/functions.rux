// Regular function
func Add(x, y: int32) -> int32 {
    return x + y;
}

// Generic function
func Div<T>(x, y: T) -> T {
    return x / y;
}

func Main() -> int32 {
    let sum = Add(5, 10);
    let quotient = Div(10.0, 2.0);
    PrintLn("Sum: {}, Quotient: {}", sum, quotient);
    return 0;
}
