# Dependencies

Add a dependency:

```sh
# Latest version
rux add Json

# Specified version
rux add Math@0.1.0
```

Remove a dependency:

```sh
rux remove Json
```

Install all dependencies listed in `Rux.toml`:

```sh
rux install
```
