# Comments

Rux supports two comment styles:

```rux
// This is a line comment. Everything from // to end-of-line is ignored.

/* This is a block comment.
   Block comments support /* nesting */ freely. */
```

Doc comments (for documentation generation via `rux doc`) follow the same syntax. Nesting block comments is fully supported.
