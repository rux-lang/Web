# Source Files

Rux source files use the `.rux` extension and must be encoded in **UTF-8**. By convention, files are stored under the `Src/` directory of a package and follow **PascalCase** naming (e.g., `Main.rux`, `HttpClient.rux`).
